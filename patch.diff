diff --git a/src/App.css b/src/App.css
deleted file mode 100644
index 08a3ac9..0000000
--- a/src/App.css
+++ /dev/null
@@ -1,68 +0,0 @@
-:root {
-  font-family: system-ui, Avenir, Helvetica, Arial, sans-serif;
-  line-height: 1.5;
-  font-weight: 400;
-
-  color-scheme: light dark;
-  color: rgba(255, 255, 255, 0.87);
-  background-color: #242424;
-
-  font-synthesis: none;
-  text-rendering: optimizeLegibility;
-  -webkit-font-smoothing: antialiased;
-  -moz-osx-font-smoothing: grayscale;
-}
-
-a {
-  font-weight: 500;
-  color: #646cff;
-  text-decoration: inherit;
-}
-a:hover {
-  color: #535bf2;
-}
-
-body {
-  margin: 0;
-  display: flex;
-  place-items: center;
-  min-width: 320px;
-  min-height: 100vh;
-}
-
-h1 {
-  font-size: 3.2em;
-  line-height: 1.1;
-}
-
-button {
-  border-radius: 8px;
-  border: 1px solid transparent;
-  padding: 0.6em 1.2em;
-  font-size: 1em;
-  font-weight: 500;
-  font-family: inherit;
-  background-color: #1a1a1a;
-  cursor: pointer;
-  transition: border-color 0.25s;
-}
-button:hover {
-  border-color: #646cff;
-}
-button:focus,
-button:focus-visible {
-  outline: 4px auto -webkit-focus-ring-color;
-}
-
-@media (prefers-color-scheme: light) {
-  :root {
-    color: #213547;
-    background-color: #ffffff;
-  }
-  a:hover {
-    color: #747bff;
-  }
-  button {
-    background-color: #f9f9f9;
-  }
-}
diff --git a/src/api/.env b/src/api/.env
index 00e9721..4a48e12 100644
--- a/src/api/.env
+++ b/src/api/.env
@@ -1,4 +1,4 @@
-DB_HOST=192.168.1.2
+DB_HOST=192.168.1.3
 DB_USER=matheus
 DB_PASSWORD=1234
 DB_NAME=lan
\ No newline at end of file
diff --git a/src/api/server.js b/src/api/server.js
index 8d089dc..3293106 100644
--- a/src/api/server.js
+++ b/src/api/server.js
@@ -171,6 +171,25 @@ app.delete('/api/rmcontainer', async (req, res) => {
   }
 })
 
+//renomear container
+app.post('/api/renamecontainer', async (req,res) => {
+  try {
+    const cont_id = req.body.cont_id
+    const cont_name = req.body.cont_name
+    const query = "update user_containers set container_name = ? where id = ?" 
+    await db.execute(query, [cont_name, cont_id], (err, result) => {
+      if (err) {
+        return res.status(500).json({ message: "Erro no banco de dados", error: err.code });
+        
+      } else{
+        res.status(200).json({message: "Renomeado com sucesso"})
+      }
+    })
+  } catch (error) {
+    res.status(500).json({message: "Erro interno", errpr: error.message})
+  }
+})
+
 
 app.listen(port, () => {
   console.log(`Servidor rodando na porta ${port}`);
diff --git a/src/pages/cadastro.jsx b/src/pages/cadastro.jsx
index fd5e67d..41e3ba2 100644
--- a/src/pages/cadastro.jsx
+++ b/src/pages/cadastro.jsx
@@ -1,5 +1,5 @@
 import { useState } from 'react';
-import './styles/cadastro.css';
+//import './styles/cadastro.css';
 import axios from 'axios';
 
 function Cadastro() {
diff --git a/src/pages/home.jsx b/src/pages/home.jsx
index 20ea4fb..6fb4cf1 100644
--- a/src/pages/home.jsx
+++ b/src/pages/home.jsx
@@ -1,54 +1,73 @@
 import axios from "axios";
 import { useEffect, useState } from "react";
+import "./styles/home.css"
 
 function Home() {
-    const [containers, setContainers] = useState([]); // Inicializa containers como um array vazio
-
-    //remover containers
+    const [containers, setContainers] = useState([]);
+    //remover container
     const rmContainer = async (cont_id, cont_name) => {
         try {
-            const userID=1
-            const response=await axios.delete("http://localhost:5000/api/rmcontainer", {
-                params: {cont_id,
-                cont_name}
+            await axios.delete("http://localhost:5000/api/rmcontainer", {
+                params: { cont_id, cont_name }
             });
-        fetchContainers();
-        } catch(error) {
-            alert("Erro: " + (error.response?.data?.message || error.message))
+            fetchContainers();
+        } catch (error) {
+            alert("Erro: " + (error.response?.data?.message || error.message));
         }
-    }
-    //listar containers
+    };
+    //atualizar lista
     const fetchContainers = async () => {
         try {
-            const userID = 1
-            const response = await axios.get("http://localhost:5000/api/listcontainers?userID=1", { userID });
-            console.log((response.data))
+            const response = await axios.get("http://localhost:5000/api/listcontainers?userID=1");
             setContainers(response.data);
         } catch (error) {
             alert("Erro ao buscar containers: " + (error.response?.data?.message || error.message));
         }
     };
-
-    //Botão de criar container
+    //criar container
     const criarContainer = async () => {
         try {
-            const userID = 1
             const response = await axios.post("http://localhost:5000/api/createcontainer", {
-                userID
+                userID: 1
             });
 
-            alert("Container criado: " + response.data.containerId);
             fetchContainers();
         } catch (error) {
             alert("Erro: " + (error.response?.data?.message || error.message));
         }
     };
+    //renomear container
+    const [editando, setEditando] = useState(null);
+    const [novoNome, setNovoNome] = useState("");
+
+    const renameContainer = async (id, novoNome) => {
+        try {
+            const response = await axios.post("http://localhost:5000/api/renamecontainer", {
+                cont_id: id,
+                cont_name: novoNome
+            });
+            fetchContainers();
+        } catch (error) {
+            alert("Erro ao renomear");
+        }
+    };
+
+    const iniciarEdicao = (id, nomeAtual) => {
+        setEditando(id);
+        setNovoNome(nomeAtual);
+    };
 
+    const salvarNome = (id) => {
+        setContainers(containers.map(container =>
+            container.id === id ? { ...container, container_name: novoNome } : container
+        ));
+        renameContainer(id, novoNome);
+        setEditando(null);
+    };
     useEffect(() => {
         fetchContainers();
     }, []);
 
-
     return (
         <div>
             <div>
@@ -58,13 +77,32 @@ function Home() {
             <h2>Containers:</h2>
             <ul>
                 {containers.map((container) => (
-                    <li key={container.id}><a href={"http://localhost:"+container.container_port}> Nome: {container.container_name} | Status: {container.container_status}</a> <button>Renomear</button> <button onClick={() =>rmContainer(container.id,container.container_name)}>Excluir</button>  </li>
+                    <li key={container.id}>
+                        {editando === container.id ? (
+                            <>
+                                <input
+                                    type="text"
+                                    value={novoNome}
+                                    onChange={(e) => setNovoNome(e.target.value)}
+                                />
+                                <button onClick={() => salvarNome(container.id)}>Salvar</button>
+                                <button onClick={() => setEditando(null)}>Cancelar</button>
+                            </>
+                        ) : (
+                            <>
+                                <a href={"http://localhost:" + container.container_port}>
+                                    {container.container_name}
+                                </a>
+                                <button onClick={() => iniciarEdicao(container.id, container.container_name)}>Renomear</button>
+                                <button onClick={() => rmContainer(container.id, container.container_name)}>Excluir</button>
+                            </>
+                        )}
+                    </li> 
                 ))}
             </ul>
+            {/*<iframe src="http://localhost:7000/" width="800" height="600"></iframe>*/}
         </div>
     );
-
 }
 
-
-export default Home;
\ No newline at end of file
+export default Home;
diff --git a/src/pages/login.jsx b/src/pages/login.jsx
index 6033de8..9cb6763 100644
--- a/src/pages/login.jsx
+++ b/src/pages/login.jsx
@@ -1,5 +1,5 @@
 import { useState } from 'react';
-import './styles/login.css';
+//import './styles/login.css';
 import axios from 'axios';
 
 function Login() {
